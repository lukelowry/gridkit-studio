{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run GridKit Studio",
      "type": "extensionHost",
      "request": "launch",
      "args": ["--extensionDevelopmentPath=${workspaceFolder}", "${workspaceFolder}/cases"],
      "outFiles": ["${workspaceFolder}/dist/**/*.cjs"],
      "preLaunchTask": "Build extension"
    }
  ]
}
